#pragma once
#include <vector>
#include <list>


int linear_search(std::vector<int>& items, int& target, size_t pos_first);

void insertion_sort(std::list<int>& num_list);
void print_vector(const std::vector<int>& v);

void print_list(const std::list<int>& lst);
